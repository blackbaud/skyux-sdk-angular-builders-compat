{
  "$schema": "http://json-schema.org/schema",
  "properties": {
    "skyuxCiPlatform": {
      "type": "string",
      "description": "The name of the continuous integration platform that will run the tests.",
      "enum": ["ado", "gh-actions"]
    },
    "skyuxHeadless": {
      "type": "boolean",
      "description": "Specifies if the test browser should be run in 'headless' mode."
    }
  }
}
